import React from 'react'
import {Layout} from "antd";

const {Footer} = Layout;

const CustomFooter = (props) => {


    return (
        <Footer style={{textAlign: 'center', position: 'absolute', bottom: "0", width: "100%"}}>SALEKA IT proposed by
            Bruce SIANI</Footer>
    )
};

export default CustomFooter